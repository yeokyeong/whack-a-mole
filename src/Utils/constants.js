const maxSeconds = 60000;
const showSeconds = {
  min: 1000,
  max: 3000
};
const hideSeconds = 500;
const maxMoleCnt = 5;
const moleCnt = 24;
const initialMoles = Array(moleCnt).fill(false);

export {
  maxSeconds,
  showSeconds,
  hideSeconds,
  maxMoleCnt,
  moleCnt,
  initialMoles
};
